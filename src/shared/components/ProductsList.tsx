import api from '../../Api'

export default function ProductsList() {

    interface Iproduct {
        product_id: string;
        product_name: string;
        product_description: string;
        product_number: string;
        color: string;
        quantity: number;
        price: string;
    }

}